---
phase: 04-ui-polish-demo-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "The app renders without horizontal scroll or layout overflow at 1280×800"
    - "The demo birthday produces a psychological profile with named stars (not fallback generic text) and strong archetypal contrast between the three paths"
    - "The presenter knows which birthday to type into the form before the demo"
  artifacts:
    - path: "public/fallback-demo.json"
      provides: "Contains the verified demo birthday in demoBirthday field"
      contains: "demoBirthday"
  key_links:
    - from: "DEMO-04 verified birthday"
      to: "public/fallback-demo.json demoBirthday field"
      via: "update fallback JSON after birthday is confirmed"
      pattern: "demoBirthday"
---

<objective>
Verify the app at 1280×800 resolution and select + vet the demo birthday to ensure a compelling profile and strong archetypal path contrasts.

Purpose: DEMO-03 and DEMO-04 are inherently human-in-the-loop tasks — one requires visual browser inspection, the other requires judgment about which psychological profile output is most compelling for a live audience.
Output: Confirmed 1280×800 layout correctness, a selected demo birthday, and an updated demoBirthday field in public/fallback-demo.json.
</objective>

<execution_context>
@/Users/xps/Desktop/ralphthon-project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xps/Desktop/ralphthon-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-ui-polish-demo-hardening/04-UI-SPEC.md
@.planning/phases/04-ui-polish-demo-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select and record the demo birthday</name>
  <files>public/fallback-demo.json</files>
  <read_first>
    - public/fallback-demo.json (read current demoBirthday field)
    - .planning/phases/04-ui-polish-demo-hardening/04-RESEARCH.md (section: Pitfall 6, Open Questions item 2)
    - lib/tuvi/index.ts (to understand calculateChart entry point)
    - lib/tuvi/profile-extractor.ts (to understand which stars produce the richest profile prose — look for the lookup tables keyed by star name)
  </read_first>
  <action>
**Step 1 — Identify which stars produce rich profile prose:**

Read `lib/tuvi/profile-extractor.ts` and locate the lookup tables that map star names to `relationalPattern` and `ambitionStructure` prose. Note which star names produce the longest, most evocative prose (stars like Phá Quân, Tham Lang, Cự Môn are expected to be richer than Thiên Đồng per RESEARCH.md).

**Step 2 — Calculate charts for candidate birthdays:**

Using tsx (since the codebase uses ES module TypeScript), run from project root:
```bash
cd /Users/xps/Desktop/ralphthon-project && npx tsx -e "
import { calculateChart } from './lib/tuvi/index';
const candidates = [
  { date: { year: 1985, month: 9, day: 15 }, birthplace: 'Ha Noi', hour: 10, decision: 'test' },
  { date: { year: 1990, month: 3, day: 21 }, birthplace: 'Ha Noi', hour: 10, decision: 'test' },
  { date: { year: 1988, month: 7, day: 7 }, birthplace: 'Ha Noi', hour: 10, decision: 'test' },
];
for (const c of candidates) {
  const result = calculateChart(c);
  const d = c.date;
  const key = d.year + '-' + String(d.month).padStart(2,'0') + '-' + String(d.day).padStart(2,'0');
  console.log(key, '→ dominant:', result.profile.dominantPalace, '| risk:', result.profile.riskStarName, '| relational:', result.profile.relationalPattern?.slice(0,80), '| ambition:', result.profile.ambitionStructure?.slice(0,80));
}
"
```

**Step 3 — Select the best birthday:**

Choose the birthday where:
- `riskStarName` is NOT null and is one of the named evocative stars
- `relationalPattern` prose is substantive (more than 20 words)
- `ambitionStructure` prose is substantive and contrasts well with relationalPattern

**Step 4 — Update public/fallback-demo.json:**

Update the `demoBirthday` field to match the selected birthday in YYYY-MM-DD format. Also ensure the node content in the `paths` object is consistent with the chosen profile's themes (the content was written for 1990-03-21 — if a different birthday is chosen, revise the node content to match the profile's dominant themes).

If 1990-03-21 is confirmed as the best option, the demoBirthday field is already correctly set and no content change is needed.
  </action>
  <verify>
    <automated>node -e "const d = require('/Users/xps/Desktop/ralphthon-project/public/fallback-demo.json'); const bd = d.demoBirthday; console.log('demoBirthday:', bd); if (!bd || bd === 'YYYY-MM-DD') process.exit(1); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `public/fallback-demo.json` contains a `demoBirthday` field with a real date (not the placeholder `YYYY-MM-DD`)
    - The selected birthday has been verified to produce a non-fallback profile (riskStarName is not null, relationalPattern is substantive)
  </acceptance_criteria>
  <done>A specific demo birthday has been selected, verified to produce compelling Tử Vi profile output, and recorded in public/fallback-demo.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify 1280x800 layout and full visual pass</name>
  <files>— (no files modified — this is a verification checkpoint)</files>
  <action>
Human verifies the complete Phase 4 visual and functional output at demo resolution. See how-to-verify for step-by-step instructions. No code changes in this task — it is a blocking gate that pauses execution for user confirmation.
  </action>
  <verify>
    <automated>cd /Users/xps/Desktop/ralphthon-project && npx tsc --noEmit && echo "TypeScript clean"</automated>
  </verify>
  <done>User has confirmed the app looks correct at 1280x800 and all visual/interaction requirements are satisfied.</done>
  <what-built>
    Plans 01 and 02 have applied the full Vietnamese lacquerware visual layer and demo hardening:
    - Paper texture grain on body background
    - Inset red-lacquer box-shadow on main container
    - Gold decorative rule below header
    - Per-element ash-state color transitions on abandoned paths (replacing flat opacity-40)
    - Three SVG bezier branch connector lines animating on mount
    - API fallback with 8-second timeout loading public/fallback-demo.json
    - ?reset URL handler clearing all session state

    Task 1 of this plan has identified and recorded the demo birthday in public/fallback-demo.json.
  </what-built>
  <how-to-verify>
    **Step 1 — Start the development server:**
    ```bash
    cd /Users/xps/Desktop/ralphthon-project && npm run dev
    ```
    Open http://localhost:3000

    **Step 2 — Set viewport to 1280x800 (DEMO-03):**
    - In Chrome DevTools: Toggle Device Toolbar, set custom dimensions to 1280 x 800
    - Check: no horizontal scrollbar appears
    - Check: all three path columns are fully visible without horizontal overflow
    - Check: the grief archive sidebar fits alongside the columns (combined width under 1280px)

    **Step 3 — Verify paper texture and altar aesthetic (UI-02, UI-03):**
    - The body background should have a subtle grain texture visible on the dark background
    - A thin gold horizontal line should appear at the top of the content area
    - The overall feel should be dark lacquerware with warm accents

    **Step 4 — Cast a chart using the demo birthday (DEMO-04):**
    - Enter the birthday from `public/fallback-demo.json` → `demoBirthday` field
    - Enter any birthplace (e.g., "Ha Noi") and any life decision
    - Click "Cast Chart"
    - Inspect the psychological profile: does it mention a specific named star? Is the relational pattern prose substantive and evocative?

    **Step 5 — Verify SVG branch connectors (UI-04):**
    - After chart is cast, watch for three gold bezier lines drawing from above the path grid outward to each column
    - Lines should animate smoothly over ~1.4 seconds
    - Lines should be gold (#C9A84C), thin (1px), no fill

    **Step 6 — Verify ash-state transition (UI-05):**
    - After paths have generated at least one node each, click "Abandon" on one path
    - Complete the grief interview (3 questions)
    - Verify the abandoned column header, depth dots, and node borders fade to ash gray (#6B6B6B family) rather than just dimming
    - The transition should take approximately 700ms

    **Step 7 — Verify ?reset (DEMO-02):**
    - Navigate to http://localhost:3000?reset
    - The app should immediately return to the input form with no chart, no paths, no grief archive visible
    - The URL bar should show http://localhost:3000 (query param removed)

    Report any layout breaks, visual issues, or interaction problems.
  </how-to-verify>
  <resume-signal>
    Type "approved" to complete Phase 4.
    Or describe any visual/functional issues found — Claude will create gap closure plans to address them.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification at 1280x800 is the primary gate for DEMO-03. The automated check verifies TypeScript remains clean.

```bash
# Verify fallback JSON has real demoBirthday
node -e "const d = require('/Users/xps/Desktop/ralphthon-project/public/fallback-demo.json'); console.log('demoBirthday:', d.demoBirthday); console.log('paths keys:', Object.keys(d.paths));"

# Final TypeScript check across all phase 4 changes
cd /Users/xps/Desktop/ralphthon-project && npx tsc --noEmit
```
</verification>

<success_criteria>
- Demo birthday is identified, recorded in public/fallback-demo.json, and produces a compelling profile
- App renders without overflow at 1280x800
- Human approves visual/interaction verification
- Phase 4 requirements UI-01 through UI-07 and DEMO-01 through DEMO-04 are all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish-demo-hardening/04-03-SUMMARY.md`
</output>
