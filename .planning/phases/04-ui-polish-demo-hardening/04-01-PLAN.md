---
phase: 04-ui-polish-demo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/PathColumn.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-05
  - UI-06
  - UI-07

must_haves:
  truths:
    - "Body has a subtle paper/grain texture visible against the dark background"
    - "The main container has a faint inner red-lacquer glow on its edges"
    - "A gold decorative rule appears below the page header"
    - "Abandoned path columns show ash-colored headers, dots, and borders instead of a flat opacity dimming"
    - "Depth dots and numeric elements use tabular-nums"
    - "No border-radius appears anywhere in the path column UI"
  artifacts:
    - path: "app/globals.css"
      provides: "Paper texture data URI on body, box-shadow on main, altar scroll CSS"
      contains: "feTurbulence"
    - path: "app/layout.tsx"
      provides: "Gold decorative rule below header area"
      contains: "border-verse-gold/20"
    - path: "components/PathColumn.tsx"
      provides: "Per-element ash-state color transitions replacing flat opacity-40"
      contains: "transition-colors duration-700"
  key_links:
    - from: "components/PathColumn.tsx"
      to: "tailwind.config.ts verse-ash token"
      via: "className conditional on isAbandoned"
      pattern: "verse-ash"
    - from: "app/globals.css"
      to: "body element"
      via: "background-image data URI"
      pattern: "feTurbulence"
---

<objective>
Apply the full Vietnamese lacquerware visual layer to the base layout and the path column component.

Purpose: The app currently has correct dark colors but no texture, no altar scroll framing, and uses a flat opacity-40 on abandoned paths rather than the per-element ash-color transition specified in the UI-SPEC.
Output: Paper texture on body, altar scroll box-shadow on main, gold decorative rule in layout, and precise ash-state color transitions in PathColumn.
</objective>

<execution_context>
@/Users/xps/Desktop/ralphthon-project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xps/Desktop/ralphthon-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-polish-demo-hardening/04-UI-SPEC.md
@.planning/phases/04-ui-polish-demo-hardening/04-RESEARCH.md
</context>

<interfaces>
<!-- Key existing patterns the executor must respect. -->

From tailwind.config.ts (verse-* tokens already configured):
- `verse-black`: `#1A0A00`
- `verse-red`: `#8B1A1A`
- `verse-gold`: `#C9A84C`
- `verse-ash`: `#6B6B6B`
- `verse-paper`: `#F5ECD7`

From components/PathColumn.tsx (current abandoned-state — line 37):
```tsx
<div className={`flex flex-col ${pathState.isAbandoned ? 'opacity-40' : ''}`}>
```
This flat opacity-40 wrapper must be REMOVED and replaced with per-element color transitions.

From app/layout.tsx (current main element — line 25):
```tsx
<main className="mx-auto max-w-5xl px-4 py-8">
```

From app/globals.css (current body — lines 11-13):
```css
body {
  background-color: #1A0A00;
  color: #F5ECD7;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Paper texture, altar scroll glow, gold rule</name>
  <files>app/globals.css, app/layout.tsx</files>
  <read_first>
    - app/globals.css (read current state before modifying)
    - app/layout.tsx (read current state before modifying)
    - .planning/phases/04-ui-polish-demo-hardening/04-UI-SPEC.md (sections: Paper Texture, Altar Scroll Layout, Color)
    - .planning/phases/04-ui-polish-demo-hardening/04-RESEARCH.md (Pattern 2, Pattern 6)
  </read_first>
  <action>
**globals.css — add paper texture data URI and altar scroll shadow:**

Replace the existing `body` block with:
```css
body {
  background-color: #1A0A00;
  color: #F5ECD7;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.05'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-size: 200px 200px;
}
```

Add after the body block:
```css
main {
  box-shadow: inset 0 0 60px rgba(139, 26, 26, 0.08);
}
```

**layout.tsx — add gold decorative rule below page header slot:**

The current `<main>` element has no inner structure. Wrap children with a fragment that adds a top gold rule. Change:
```tsx
<main className="mx-auto max-w-5xl px-4 py-8">
  {children}
</main>
```
To:
```tsx
<main className="mx-auto max-w-5xl px-4 py-8">
  <div className="border-b border-verse-gold/20 mb-8" />
  {children}
</main>
```

This thin 1px gold rule appears at the very top of the content area, acting as the altar scroll top border. The `mb-8` (32px) gives breathing room before the page content.
  </action>
  <verify>
    <automated>grep -n "feTurbulence" /Users/xps/Desktop/ralphthon-project/app/globals.css && grep -n "rgba(139, 26, 26" /Users/xps/Desktop/ralphthon-project/app/globals.css && grep -n "border-verse-gold/20" /Users/xps/Desktop/ralphthon-project/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains the string `feTurbulence`
    - `app/globals.css` contains `background-size: 200px 200px`
    - `app/globals.css` contains `box-shadow: inset 0 0 60px rgba(139, 26, 26, 0.08)` on `main`
    - `app/layout.tsx` contains `border-verse-gold/20`
    - `app/layout.tsx` contains `mb-8`
  </acceptance_criteria>
  <done>Body has paper texture grain overlay. Main container has subtle red-lacquer inset glow. A 1px gold rule appears at the top of the content area as the altar scroll top border.</done>
</task>

<task type="auto">
  <name>Task 2: PathColumn ash-state per-element color transitions</name>
  <files>components/PathColumn.tsx</files>
  <read_first>
    - components/PathColumn.tsx (read full file before modifying)
    - .planning/phases/04-ui-polish-demo-hardening/04-UI-SPEC.md (sections: Color — ash state transition, Interaction States — Abandoned Path)
    - .planning/phases/04-ui-polish-demo-hardening/04-RESEARCH.md (Pattern 5, code examples for ash-state)
  </read_first>
  <action>
**Remove the flat opacity-40 wrapper and replace with per-element conditional color classes.**

1. **Wrapper div (line 37):** Remove `opacity-40` conditional entirely:
   ```tsx
   // BEFORE:
   <div className={`flex flex-col ${pathState.isAbandoned ? 'opacity-40' : ''}`}>
   // AFTER:
   <div className="flex flex-col">
   ```

2. **Column header h2 (line 39):** Add `transition-colors duration-700` and conditional gold/ash color:
   ```tsx
   // BEFORE:
   <h2 className="text-xs font-light tracking-[0.4em] uppercase text-verse-gold/70 mb-4">
   // AFTER:
   <h2 className={`text-xs font-light tracking-[0.4em] uppercase mb-4 transition-colors duration-700 ${pathState.isAbandoned ? 'text-verse-ash/50' : 'text-verse-gold/70'}`}>
   ```

3. **Depth indicator dots (line 48):** Add `transition-colors duration-500` and conditional ash/gold:
   ```tsx
   // BEFORE:
   className={i < currentDepth ? 'text-verse-gold' : 'text-verse-paper/30'}
   // AFTER:
   className={`transition-colors duration-500 ${i < currentDepth ? (pathState.isAbandoned ? 'text-verse-ash/60' : 'text-verse-gold') : 'text-verse-paper/30'}`}
   ```

4. **Committed node left-border (line 65):** Add conditional ash/gold border:
   ```tsx
   // BEFORE:
   className="border-l-2 border-verse-gold/30 pl-3"
   // AFTER:
   className={`border-l-2 pl-3 transition-colors duration-700 ${pathState.isAbandoned ? 'border-verse-ash/20' : 'border-verse-gold/30'}`}
   ```

5. **tabular-nums on depth dots div (line 44):** Add `tabular-nums` class to the depth dots container:
   ```tsx
   // BEFORE:
   <div className="flex gap-1.5 my-3">
   // AFTER:
   <div className="flex gap-1.5 my-3 tabular-nums">
   ```

Do NOT change the streaming node border (line 83 `border-verse-gold/40`) — it only shows while streaming which only happens on active non-abandoned paths.

Do NOT add border-radius anywhere. The lacquerware aesthetic is rectilinear — no `rounded-*` classes.
  </action>
  <verify>
    <automated>grep -n "transition-colors duration-700" /Users/xps/Desktop/ralphthon-project/components/PathColumn.tsx && grep -n "verse-ash" /Users/xps/Desktop/ralphthon-project/components/PathColumn.tsx && grep -n "tabular-nums" /Users/xps/Desktop/ralphthon-project/components/PathColumn.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/PathColumn.tsx` does NOT contain `opacity-40` (the flat wrapper dimming is removed)
    - `components/PathColumn.tsx` contains `verse-ash/50` (header ash color)
    - `components/PathColumn.tsx` contains `verse-ash/60` (depth dot ash color)
    - `components/PathColumn.tsx` contains `verse-ash/20` (node border ash color)
    - `components/PathColumn.tsx` contains `transition-colors duration-700` (smooth color fade)
    - `components/PathColumn.tsx` contains `tabular-nums`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Abandoned paths transition their header text, depth dots, and node borders from gold to ash over 700ms. No flat opacity dimming. Depth dots use tabular-nums rendering.</done>
</task>

</tasks>

<verification>
```bash
# Check paper texture is in globals.css
grep "feTurbulence" /Users/xps/Desktop/ralphthon-project/app/globals.css

# Check altar scroll shadow is in globals.css
grep "rgba(139, 26, 26" /Users/xps/Desktop/ralphthon-project/app/globals.css

# Check gold rule in layout
grep "border-verse-gold/20" /Users/xps/Desktop/ralphthon-project/app/layout.tsx

# Check ash-state transitions in PathColumn
grep "verse-ash" /Users/xps/Desktop/ralphthon-project/components/PathColumn.tsx

# Check no flat opacity-40 wrapper remains
grep "opacity-40" /Users/xps/Desktop/ralphthon-project/components/PathColumn.tsx | grep "isAbandoned"
# should return nothing — the wrapper opacity-40 is removed

# TypeScript check
cd /Users/xps/Desktop/ralphthon-project && npx tsc --noEmit
```
</verification>

<success_criteria>
- `app/globals.css` has paper texture via feTurbulence data URI on body
- `app/globals.css` has inset red-lacquer box-shadow on main
- `app/layout.tsx` has 1px gold decorative rule at top of content
- `components/PathColumn.tsx` has per-element ash transitions replacing flat opacity-40
- `components/PathColumn.tsx` has tabular-nums on depth dots
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish-demo-hardening/04-01-SUMMARY.md`
</output>
